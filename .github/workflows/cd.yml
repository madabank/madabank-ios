name: Madabank CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ develop ]
  workflow_dispatch:

jobs:
  deploy_beta:
    runs-on: macos-15
    
    permissions:
      contents: write

    env:
      APPLE_ID: ${{ secrets.APPLE_ID }}
      APP_STORE_CONNECT_API_KEY_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_KEY_ID }}
      APP_STORE_CONNECT_API_KEY_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ISSUER_ID }}
      APP_STORE_CONNECT_API_KEY_KEY: ${{ secrets.APP_STORE_CONNECT_API_KEY_KEY }}
      MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
      MATCH_GIT_URL: ${{ secrets.MATCH_GIT_URL }}
      GH_TOKEN: ${{ secrets.GH_TOKEN }} # For Match git access
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      CI_PAT: ${{ secrets.CI_PAT }}
      GIT_AUTHORIZATION: ${{ secrets.GIT_AUTHORIZATION }}
      APP_IDENTIFIER: online.darisadam.madabank
      TEAM_ID: Q99UJS4WY6
      
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          token: ${{ secrets.GH_TOKEN }}
      
      - name: Select Xcode
        run: |
          sudo xcode-select -s /Applications/Xcode_16.2.app
          xcodebuild -version

      - name: Install iOS Simulator Runtime (if needed)
        run: |
          # Install the latest iOS runtime to match SDK
          xcodebuild -downloadAllPlatforms || true
          xcrun simctl list runtimes
        
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '4.0.1'
          bundler-cache: true
          working-directory: Madabank

      - name: Install Tuist
        run: |
          curl https://mise.run | sh
          export PATH="$HOME/.local/bin:$PATH"
          mise use -g tuist
          # Add shims to PATH for the current step
          export PATH="$HOME/.local/share/mise/shims:$PATH"
          # Add shims to GITHUB_PATH for subsequent steps
          echo "$HOME/.local/share/mise/shims" >> $GITHUB_PATH
          tuist version

      - name: Generate Project
        run: |
          cd Madabank
          tuist install
          tuist generate
          
      - name: Configure Git for Push
        run: |
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          git remote set-url origin https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/${{ github.repository }}.git

      - name: Run Fastlane Beta
        run: |
          cd Madabank
          bundle exec fastlane beta
