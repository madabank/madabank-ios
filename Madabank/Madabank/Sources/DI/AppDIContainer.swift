import Foundation

/// Dependency Injection Container for the Madabank app
final class AppDIContainer {
    
    // MARK: - Shared Services
    
    // TODO: Add services from Data module
    // lazy var authRepository: AuthRepository = AuthRepositoryImpl(apiClient: apiClient)
    // lazy var accountRepository: AccountRepository = AccountRepositoryImpl(apiClient: apiClient)
    
    // MARK: - Network
    
    // TODO: Add API client from Network module
    // lazy var apiClient: APIClient = APIClient(baseURL: Environment.apiBaseURL)
    
    // MARK: - Init
    
    init() {
        // Configure dependencies
    }
}
